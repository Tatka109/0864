
let height = prompt ('1.73');
alert('1.73 : ' + height);

let weight = prompt ('84');
alert ('84: ' + weight);

let fatmass = prompt ('5');
alert ('5' + fatmass);

alert (84 / (1.73 ^ 2)); // 28; 

console.log (28);

alert ((5 / 84)) * 100;//

console.log (5.95)


const name = prompt ('Какая температура?');
var temperature = 20;

const result = confirm ('Идет дождь?');

if (itsRaining || temperature < 0 || temperature > 35)
  minutes = 0;
else if (temperature <= 20)
  minutes = temperature;
else if (temperature > 20)
  minutes = 20 * 2 - temperature;


var itsRaining = false;

var minutes;



 console.log('Time to walk = ' + minutes +' min')

 ************

 alert ('Температура: ' + prompt ('Какая температура?'));
 var temperature = ('20');
 var itsRaining = confirm ('Идет дождь?')
 
 if (itsRaining ==true) {
    (itsRaining && temperature < 0 ** temperature > 35)
    minutes = 0;
    console.log (0);
 }

 else {
    (temperature <= 20 && temperature >= 20);
 }
 console.log (20 * 2 - temperature);


//////

alert ('Температура: ' + prompt ('Какая температура?'));
var itsRaining = confirm ('Идет дождь?')
var temperature = 20;
var minutes=20;

var itsRaining = true;  

if (itsRaining || temperature >= 35 || temperature <= 0) {minutes=0}

else if (temperature<=20) {minutes=temperature}

else (minutes=minutes*2-temperature)

console.log('Минут на прогулку '+minutes);


